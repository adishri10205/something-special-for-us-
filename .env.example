# ============================================
# FIREBASE CONFIGURATION
# ============================================
# Get these from: https://console.firebase.google.com/project/YOUR_PROJECT/settings/general

VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# ============================================
# MUX API CREDENTIALS (OPTIONAL)
# ============================================
# WARNING: These should ONLY be used in a backend server, NOT in the frontend!
# For production, set up Firebase Functions or a backend server to handle Mux API calls
# Get these from: https://dashboard.mux.com/settings/access-tokens

VITE_MUX_TOKEN_ID=your_mux_token_id_here
VITE_MUX_SECRET_KEY=your_mux_secret_key_here

# ============================================
# IMPORTANT SECURITY NOTES
# ============================================
# 1. Copy this file to .env and fill in your actual values
# 2. NEVER commit .env file to Git (it's in .gitignore)
# 3. NEVER share these credentials publicly
# 4. In Vite, env vars must be prefixed with VITE_ to be accessible in the browser
# 5. SECRET_KEY should NEVER be in the frontend - this is just for development/testing
# 6. For production, move all secret keys to a backend server
# 7. Rotate your API keys immediately if they've been exposed
